# HANDOFF — U23 (A→D) chốt phiên 2026-07-16

> Bàn giao phiên điều phối U23. **A→D (4 tinh chỉnh khách hàng) HOÀN TẤT & merge-ready.**
> **Admin (E = U17→U21) TÁCH sang phiên chuyên biệt** (quyết định chủ dự án).
> Nhật ký chi tiết từng bước: [`U23-tien-do.md`](./U23-tien-do.md). Spec nguồn:
> `U23-plan-4-tinh-chinh.md` + `U23-prompts.md` (ở MAIN worktree, untracked).

## 1. Nhánh & trạng thái

- **Nhánh:** `claude/u23-unit-execution-59b6aa` (worktree `.claude/worktrees/u23-unit-execution-59b6aa`).
- **Base:** `feat/cloudflare-stack-u0`. Nhánh **đứng trước base 37 commit** (12 commit U23 + nền fordex/deploy có sẵn).
- **Cây làm việc:** sạch. `make lint` EXIT=0, `make test` EXIT=0 (toàn workspace).
- **CHƯA tạo PR, CHƯA merge, CHƯA deploy.**

## 2. Đã làm — A→D (khách hàng)

| ĐV | Nội dung | Commit(s) | QA |
|---|---|---|---|
| **A** | Bảng dòng hàng màn Chi tiết (đã có sẵn từ `2674378` — tinh chỉnh chữ rỗng + siết test 7 cột/>2^53) | `53036e8` | dod-auditor ĐẠT |
| **B** | Dòng hàng vào kết xuất: xlsx **sheet 2** "Chi tiết dòng hàng" + csv **khối 2**; tái dùng `fetchLinesForInvoices` | `9fe2fb5` | dod + security ĐẠT |
| **C** | Dashboard tối giản: bỏ số tiền + đối chiếu, giữ **1 dòng trạng thái kết nối** từ `GET /tax-accounts` | `b16572e` | dod-auditor ĐẠT |
| **D1** | Migration `0006` UNIQUE(mst) + UNIQUE(tenant_id, username) (idempotent) | `df8f0a4` | — |
| **D2** | `POST /tax-accounts`: chính auto `username=tenants.mst`; hạn mức `getGioiHanTkThue` (tạm=1)→409; con ẩn sau cờ (TẮT)+`isValidSubUsername` | `80642e3` | dod + security ĐẠT |
| **D3** | Seam `clearToken` + `POST /:id/disconnect` (audit `ngat_ket_noi_thue`, cách ly→404, giữ MST) | `df0792e` | dod + security ĐẠT |
| **D4** | Màn kết nối rút gọn: MST read-only che từ `/me`, nút Ngắt kết nối (xác nhận), khối con ẩn sau cờ, mật khẩu không lưu client | `e0f22e9` | dod + security ĐẠT |

Docs điều phối: `c40de1b`, `c9851ca`, `9faf20a`, `1c16117`, `8c79cb7`.

**Hợp đồng API đổi (đã cập nhật `docs/06-BINDING_MAP.md` từ mã):** `POST /tax-accounts` (auto MST, 409 hạn mức, con sau cờ) + route mới `POST /tax-accounts/:id/disconnect`; Dashboard nay chỉ dùng `GET /tax-accounts`; `GET /invoices/:id` trả `dongHangHoa`.

## 3. Việc tiếp theo — Admin (E), PHIÊN RIÊNG

Thứ tự phụ thuộc **U17 → U18 → U19 → U21** (plans ở MAIN worktree `docs/plans/U17..U21-plan.md`, untracked):

1. **U17** (BE): đăng ký công khai + duyệt + **gói dịch vụ**. Cũng là nơi:
   - thay hardcode `getGioiHanTkThue(tenant)` (`apps/api/src/routes/taxAccounts.ts`) bằng nguồn gói dịch vụ;
   - xử **nợ TOCTOU hạn mức** khi bật module tài khoản con (task chip `task_230e0541`).
2. **U18** (BE): Admin API **super-admin XUYÊN-TENANT** — **đơn vị nhạy cảm bảo mật nhất**. Danh tính/đăng nhập/token admin **TÁCH khỏi bảng khách**; audit mọi truy cập xuyên-tenant; **KHÔNG** lộ token thuế/nội dung hóa đơn của tenant. **Bắt buộc security-reviewer.**
3. **U19** (FE): cổng Admin `/admin` tách biệt, đăng nhập tách hẳn khỏi khách.
4. **U21** (FE): Dashboard giám sát Admin (số liệu xuyên-tenant qua super-admin U18).

## 4. Nợ & rủi ro đã ghi

- **TOCTOU hạn mức (Low)** — `POST /tax-accounts` đếm rồi insert không khóa. Hiện AN TOÀN cho tài khoản chính (đều dùng `mst` → `UNIQUE(tenant_id,username)` chặn). Rủi ro chỉ khi **bật module tài khoản con** → khóa đếm (`SELECT … FOR UPDATE`) khi làm U17. Chip: `task_230e0541`.
- **Module tài khoản con** = dựng nền, cờ mã **mặc định TẮT** (BE `SUB_ACCOUNT_MODULE_ENABLED`, FE `SUB_ACCOUNT_UI_ENABLED`). Bật khi gói dịch vụ cho phép (U17).

## 5. Vận hành (bắt buộc)

- **Worktree mới CẦN `npm ci`** trước khi tin `make lint`/`make test` — worktree lồng trong repo chính resolve `@vat/*` về packages cũ của main → tsc lỗi giả. (Đã xác lập phiên này.)
- **Deploy (`.claude/rules/deploy.md`):** migration `0006` **PHẢI `make migrate` TRƯỚC** khi deploy `apps/api` (code đọc ràng buộc mới) — không đảo ngược thứ tự. Sau deploy smoke-test đúng đường `POST /tax-accounts`, không chỉ `/health`.

## 6. Cách resume

Phiên sau: đọc [`U23-tien-do.md`](./U23-tien-do.md) (§Bàn giao) + file này → chọn đơn vị Admin kế → chạy `/start-unit U17` (skill tự đọc spec). Memory: `vat-u23-2026-07-16`.
