# HANDOFF — Chốt phiên 2026-07-20 (H-B.6 ra production)

> Bàn giao cho phiên sau. Đọc file này TRƯỚC, rồi `FORDEX-PROGRESS.md` (sổ trạng thái SỐNG). Phiên này đưa **H-B.6 (a)+(b)** từ spec ra **production live** + hoà giải nợ git-mess. Không lặp nội dung file kia — file này chốt phiên + gom **nợ đóng nốt**.

## 1. Phiên này đã làm gì

**Đơn vị H-B.6 — vòng khép kín DLQ + EgressHealth-gate (phạm vi a+b, hoãn c):**

| Bước | Kết quả |
|---|---|
| Spec → 2 vòng QA1 độc lập → 8 task TDD (subagent-driven, review chéo từng task) | Final review: Ready to merge |
| Hoà giải git-mess | Rebase bỏ 3 commit U27 thừa; migration `0007→0008` (journal idx 8) |
| Verify nền đã rebase | `make lint` sạch; `packages/db` 63/63; `apps/sync-worker` 117/117 (H-B.6 + U25/U28 chạy chung) |
| Merge **PR #18** → `feat/cloudflare-stack-u0` | CI xanh (quality + secret-scan PASS) |
| Migrate `0008_dong_bo_that_bai` → Neon prod | Smoke: bảng tồn tại, RLS ENABLE+FORCE=true, 9 cột, 1 policy, chain 0000-0008 |
| Deploy `vat-sync-worker` | Version `46769ea4`; **vat-sync-dlq consumer live** (0→1) |
| Hardening bảo mật Phần A | `workers_dev:false` + custom domain `vat-sync-admin.tourdao.vn` → workers.dev nay 404. **PR #19** |

**Bằng chứng vận hành:** vòng khép kín chạy thật — job rơi `vat-sync-dlq` → ghi `dong_bo_that_bai` + audit `dong_bo_that_bai_dlq`; egress GEO_BLOCKED → gate chặn enqueue (cron) + reenqueue-delay (consumer), đạt trần `bpAttempt` → `retry` → DLQ (có điểm dừng).

**Bài học kỹ thuật (đã ghi trong plan `docs/plans/H-B.6-impl.md`):**
- Test RLS cách ly PHẢI theo mẫu `constraints.test.ts` (role **nosuperuser**, kiểm CẢ ENABLE lẫn owner+FORCE) — PGlite mặc định superuser bỏ qua RLS. QA1 tĩnh không bắt được, chỉ TDD chạy thật mới lộ.
- Nhánh blocked của gate phải tôn trọng trần → DLQ, không reenqueue vô hạn (spec §4 "vòng khép kín").

## 2. Nợ đóng nốt — CHỦ DỰ ÁN (làm sớm)

- **⚠️ Bật Cloudflare Access cho `/dlq/replay`** (đã chọn "mức B — giữ + bật Access"). Access chỉ chặn được hostname trong zone `tourdao.vn` (không chặn `*.workers.dev`) → Phần A đã đưa endpoint lên `vat-sync-admin.tourdao.vn`. **Chưa bật Access** → endpoint sẽ công khai ở hostname mới khi DNS/cert lên (rủi ro thấp: ép tenantId + RLS, chỉ replay job hỏng đúng tenant). Dashboard Zero Trust → Access → Self-hosted app cho `vat-sync-admin.tourdao.vn/dlq/*` + Service Auth policy (service token cho curl).
- **Merge PR #19** (config hardening: workers_dev off + route). Nếu không, deploy sau từ `feat/cloudflare-stack-u0` bật lại workers.dev.
- **Dọn git:** nhánh backup `backup/hb6-before-rebase-20260720`; nhánh U27 trùng `feat/u27-web-loc-ketxuat-dongbo`; worktree `vat-hb6-spec` (khi production yên).

## 3. Nợ CODE — tách ra từ H-B.6 (làm sau)

- **H-B.6c** — DO global-egress quota tổng, hợp nhất BACKLOG #1 (rate-limit toàn cục GDT). Kèm: thêm **khoá dedup** cho replay (hiện mỗi lần replay tạo hàng `dong_bo_that_bai` mới, append-only, không nối DLQ-re-entry).
- **Minor tồn (ghi cho lần đụng sau, không chặn):** FK migration `0008` thiếu `public.` qualifier + named constraint (từ brief); `lyDo='max_retries'` hard-code (CHƯA KIỂM CHỨNG — CF Queues không truyền lý do); nhánh `ids` của `replayDeadLetters` chưa có test; `req.json()` endpoint replay không try/catch; log warn nhánh DLQ đã có tenantId (đã vá).

## 4. Nợ SẢN PHẨM phát hiện phiên này

- **KHÔNG có UI admin.** `/dlq/replay` chỉ gọi được bằng `curl` — không có trang quản trị. Chủ dự án muốn "dashboard quản lý mọi thứ" (xem/replay job hỏng, quản tenant/người dùng, audit) → đây là **việc sản phẩm mới**, thuộc vùng **U24** (quản lý người dùng nội bộ) + cần đặc tả riêng (super-admin đọc nhiều tenant đụng ranh giới cách ly RLS/pháp lý — xem BACKLOG). Chưa hiện thực.

## 5. Nợ dự án RỘNG hơn (không đổi so với HANDOFF-2026-07-16)

H-B.1/B.5 (index/sharding), H-C.1/C.2/C.5/C.7, **H-C.4 pháp lý NĐ13 (cần luật sư)**. **Quy trình:** git-race nhiều agent chung repo đã xảy ra ≥2 lần → nên thành luật "mỗi agent một git worktree" trong `.claude/rules/`.
