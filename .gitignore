__pycache__/
*.pyc
.venv/
venv/
.env
.env.local
.env.production
# Ngoại lệ: chỉ chứa path API same-origin (/api), KHÔNG phải secret — cần commit để build
# production luôn đúng, tránh footgun quên VITE_API_BASE (production-deploy.md §3).
!apps/web/.env.production
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Ngăn xếp Node/TypeScript/Cloudflare (ADR-0001)
node_modules/
dist/
.wrangler/
.dev.vars
*.tsbuildinfo
coverage/

# Cổng kiểm soát: log audit prompt là dữ liệu vận hành, không commit vào git.
.claude/logs/
.claude/settings.local.json

# macOS
.DS_Store
